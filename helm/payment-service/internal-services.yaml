apiVersion: v1
kind: Service
metadata:
  name: payment-service-blue-internal
  namespace: apps
spec:
  selector:
    app: payment-service
    color: blue
  ports:
  - port: 5000
    targetPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service-green-internal
  namespace: apps
spec:
  selector:
    app: payment-service
    color: green
  ports:
  - port: 5000
    targetPort: 5000