apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: apps
spec:
  selector:
    app: order-service
    color: blue       # <-- this line is what we patch to cut over
  ports:
  - port: 5000
    targetPort: 5000