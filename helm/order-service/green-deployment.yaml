apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service-green
  namespace: apps
  labels:
    app: order-service
    color: green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
      color: green
  template:
    metadata:
      labels:
        app: order-service
        color: green
    spec:
      containers:
      - name: order-service
        image: archana2026/order-service:v1
        ports:
        - containerPort: 5000
        env:
        - name: USER_SERVICE_URL
          value: "http://user-service:5000"