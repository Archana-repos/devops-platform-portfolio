apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service-blue
  namespace: apps
  labels:
    app: user-service
    color: blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
      color: blue
  template:
    metadata:
      labels:
        app: user-service
        color: blue
    spec:
      containers:
      - name: user-service
        image: archana2026/user-service:v1
        ports:
        - containerPort: 5000